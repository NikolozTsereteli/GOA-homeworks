def count_name_characters():
    # Accept a name as input
    name = input("Enter a name: ")
    
    # Calculate the number of characters in the name
    num_characters = len(name)
    
    # Print the result
    print(f"The name '{name}' has {num_characters} characters.")

# Call the function
count_name_characters()
